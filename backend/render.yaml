services:
  - type: web
    name: fitness-backend
    env: java
    buildCommand: mvn clean install -DskipTests
    startCommand: java -jar target/fitness-backend-0.0.1-SNAPSHOT.jar
    envVars:
      - key: JAVA_OPTS
        value: -Xmx512m
      - key: SERVER_PORT
        value: 7070
